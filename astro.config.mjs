// @ts-check
import { defineConfig } from 'astro/config';

// La direccion publica de la web. Con dominio propio se cambia aqui y en
// `academia.url` de src/data/site.ts.
export default defineConfig({
  site: 'https://smarter-academy.chefsitturchey.workers.dev',
});
